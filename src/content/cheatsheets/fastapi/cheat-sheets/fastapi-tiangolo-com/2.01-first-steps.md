---
title: "2.01 - Primeros Pasos en FastAPI"
description: "Guía paso a paso para crear, entender y ejecutar tu primera API"
tags: ["fastapi", "python", "intro", "http"]
prompt-version: cheat-sheet-generator-1.1
---

## A - Estructura Básica de Archivo (`main.py`) [🔴]

#### 1. **Introducción:**

Es el código mínimo necesario para que una aplicación FastAPI funcione.

#### 2. **Ejemplo:**

Copia esto en un archivo llamado `main.py`:

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
async def root():
    return {"message": "Hello World"}
```

#### 3. **Desarrollo:**

Este pequeño bloque hace tres cosas gigantes: importa la herramienta, crea la aplicación y define qué pasa cuando alguien entra a la página principal.

[🔴] [Fundamental]: Sin este esqueleto, no hay aplicación. Es la base sobre la que construirás todo lo demás.

---

## B - Ejecución del Servidor (`fastapi dev`) [🔴]

#### 1. **Introducción:**

Es el comando que escribes en tu terminal para "encender" tu aplicación y que esta empiece a escuchar peticiones.

#### 2. **Ejemplo:**

En tu terminal, ejecuta:

```bash
fastapi dev main.py
```

**Salida esperada (resumida):**

```text
INFO     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO     Started reloader process...
INFO     Application startup complete.
```

#### 3. **Desarrollo:**

Al ejecutar esto, tu computadora se convierte en un servidor local. La terminal te dirá dónde está "viviendo" tu app (usualmente `http://127.0.0.1:8000`).

- **Nota paranoica:** El comando `fastapi dev` es para **desarrollo** (se recarga si cambias el código). El texto menciona que para **producción** (cuando ya la publicas al mundo real) debes usar `fastapi run`.

[🔴] [Fundamental]: Si no ejecutas el servidor, tu código es solo texto en un archivo; no hace nada.

---

## C - Verificación y Respuesta JSON [🟡]

#### 1. **Introducción:**

Es la confirmación visual de que tu servidor funciona y responde con datos.

#### 2. **Ejemplo:**

Abre tu navegador y ve a: `http://127.0.0.1:8000`

Verás esto en la pantalla:

```json
{ "message": "Hello World" }
```

#### 3. **Desarrollo:**

FastAPI toma el diccionario de Python que escribiste en el código (`{"message": "..."}`) y lo transforma automáticamente a **JSON**, que es el lenguaje universal para que las APIs hablen con navegadores o aplicaciones móviles.

[🟡] [Importante]: Es tu primer "feedback" real de que todo salió bien.

---

## D - Documentación Interactiva (Swagger UI) [🟡]

#### 1. **Introducción:**

Una página web automática que te permite ver y probar tu API sin escribir código extra.

#### 2. **Ejemplo:**

Ve a: `http://127.0.0.1:8000/docs`

Verás esto:
![Swagger UI](https://fastapi.tiangolo.com/img/index/index-01-swagger-ui-simple.png)

#### 3. **Desarrollo:**

Esta interfaz es provista por **Swagger UI**. No tuviste que programarla; FastAPI la generó sola. Te permite enviar peticiones a tu API directamente desde el navegador para probarla.

[🟡] [Importante]: Ahorra horas de trabajo al no tener que crear documentación manual para probar tus endpoints.

---

## E - Documentación Alternativa (ReDoc) [🔵]

#### 1. **Introducción:**

Otra versión visual de la documentación, más enfocada en la lectura.

#### 2. **Ejemplo:**

Ve a: `http://127.0.0.1:8000/redoc`

Verás esto:
![ReDoc](https://fastapi.tiangolo.com/img/index/index-02-redoc-simple.png)

#### 3. **Desarrollo:**

Provista por **ReDoc**. Muestra la misma información que la anterior, pero con un diseño diferente. Es útil si prefieres una lista más limpia de tus rutas.

[🔵] [Específico]: Es bueno saber que existe si no te gusta el estilo de Swagger UI.

---

## F - OpenAPI y Schemas [🔵]

#### 1. **Introducción:**

Es el estándar (las reglas del juego) que usa FastAPI para describir tu API al mundo.

#### 2. **Ejemplo:**

Puedes ver el "esqueleto" crudo en: `http://127.0.0.1:8000/openapi.json`

#### 3. **Desarrollo:**

Aquí hay varios sub-conceptos que suelen confundir, vamos a separarlos:

- **Schema (Esquema):** Es una descripción abstracta. No es el código, es el "plano" de cómo se ve algo.
- **API Schema:** Define tus rutas (`/items`), qué parámetros reciben y qué devuelven. FastAPI usa el estándar **OpenAPI** para esto.
- **Data Schema:** Define la forma de los datos (ej: "esto es un usuario y tiene nombre y edad"). FastAPI usa **JSON Schema** para esto.

**¿Para qué sirve?** Gracias a que FastAPI genera este "Schema" automáticamente, herramientas como Swagger UI, ReDoc, o generadores de código para Frontend pueden entender tu API sin que tú hagas nada.

[🔵] [Específico]: No necesitas memorizar el JSON crudo, pero debes saber que es el motor que hace funcionar la documentación automática.

---

## G - Importar e Instanciar FastAPI [🔴]

#### 1. **Introducción:**

Los dos primeros pasos del código: traer la herramienta y crear el objeto principal.

#### 2. **Ejemplo:**

```python
from fastapi import FastAPI  # Paso 1
app = FastAPI()              # Paso 2
```

#### 3. **Desarrollo:**

1.  **Importar:** Traes la clase `FastAPI` desde la librería.
2.  **Instanciar:** Creas la variable `app`. Esta variable `app` es la jefa; será el punto principal de interacción para crear toda tu API.

[🔴] [Fundamental]: Sin la instancia `app`, no tienes dónde "pegar" tus rutas.

---

## H - Path (Ruta) [🔴]

#### 1. **Introducción:**

Es la parte de la dirección web (URL) que viene después del dominio principal.

#### 2. **Ejemplo:**

Si la URL es `https://example.com/items/foo`

El **Path** es: `/items/foo`

#### 3. **Desarrollo:**

También se le llama "endpoint" o "route". Es la forma en que separas los recursos de tu aplicación. Por ejemplo, todo lo que tenga que ver con usuarios iría en `/users`.

[🔴] [Fundamental]: Es la dirección de casa de tus funciones.

---

## I - Operation (Métodos HTTP) [🔴]

#### 1. **Introducción:**

Es la "acción" que quieres realizar en una ruta específica (Path).

#### 2. **Ejemplo:**

- `POST`: Crear datos.
- `GET`: Leer datos.
- `PUT`: Actualizar datos.
- `DELETE`: Borrar datos.

#### 3. **Desarrollo:**

En el protocolo HTTP, te comunicas con las rutas usando estos "métodos". FastAPI (y OpenAPI) los llama **Operaciones**.

**Comparación Importante:**

- Si quieres **ver** una página web o datos, usas **GET**.
- Si quieres **enviar** un formulario o crear algo nuevo, usas **POST**.
- _Trampa común:_ No uses `GET` para enviar contraseñas o crear cosas, y no uses `POST` solo para leer una lista simple (aunque técnicamente funcione, rompe las reglas semánticas).

[🔴] [Fundamental]: Debes elegir el verbo correcto para la acción correcta.

---

## J - Decorador de Operación de Ruta (`@app.get`) [🔴]

#### 1. **Introducción:**

Es la sintaxis especial (`@`) que le dice a FastAPI: "La función de abajo se encarga de esta ruta y esta operación".

#### 2. **Ejemplo:**

```python
@app.get("/")
async def root(): ...
```

#### 3. **Desarrollo:**

Imagina que el decorador es un sombrero decorativo que le pones a tu función.

- `@app`: Usa la instancia que creamos antes.
- `.get`: Define la **Operación** (método HTTP).
- `("/")`: Define el **Path** (ruta).

Le dice a FastAPI: "Cuando alguien visite `/` usando `GET`, ejecuta la función `root`".

[🔴] [Fundamental]: Es el pegamento que une la URL con tu código Python.

---

## K - Función de Operación de Ruta (Async vs Def) [🟡]

#### 1. **Introducción:**

Es la función de Python normal que contiene la lógica de qué hacer cuando llega una petición.

#### 2. **Ejemplo:**

**Opción A (Async):**

```python
async def root():
    return {"message": "Hello World"}
```

**Opción B (Normal):**

```python
def root():
    return {"message": "Hello World"}
```

#### 3. **Desarrollo:**

Esta función es llamada por FastAPI cada vez que recibe una solicitud que coincide con el decorador.

**Comparación Paranoica (Async vs Def):**
El texto hace una distinción importante aquí.

- Usa `async def` si tu código necesita esperar cosas (como bases de datos asíncronas).
- Usa `def` (normal) si no sabes qué es `async` o no lo necesitas.
- **Consejo del Pedagogo:** Si eres principiante y la palabra "asíncrono" te suena a chino, usa `def` normal. FastAPI es inteligente y hará que funcione rápido de todas formas. No te compliques ahora.

[🟡] [Importante]: Aquí es donde vive tu lógica de negocio.

---

## L - Retorno de Contenido [🟡]

#### 1. **Introducción:**

Lo que devuelve tu función se envía al cliente (navegador) como respuesta.

#### 2. **Ejemplo:**

```python
return {"message": "Hello World"} # Devuelve un diccionario (dict)
# También puedes devolver listas, strings, enteros, etc.
```

#### 3. **Desarrollo:**

FastAPI toma lo que retornes (dict, list, str, int, e incluso modelos Pydantic u ORMs) y lo convierte automáticamente a formato JSON para que el cliente pueda leerlo.

[🟡] [Importante]: La conversión automática te ahorra tener que formatear el texto manualmente.

---

## M - Despliegue (FastAPI Cloud) [🔵]

#### 1. **Introducción:**

Cómo llevar tu aplicación de tu máquina local a internet.

#### 2. **Ejemplo:**

```bash
fastapi login
fastapi deploy
```

#### 3. **Desarrollo:**

El texto menciona **FastAPI Cloud** como una opción oficial y sencilla.

1.  Te logueas.
2.  Despliegas con un comando.
3.  Obtienes una URL pública (ej: `https://myapp.fastapicloud.dev`).

_Nota:_ FastAPI es open source, así que también puedes desplegarlo en cualquier otro proveedor de nube si lo prefieres.

[🔵] [Específico]: Es opcional para aprender, pero necesario si quieres que otros usen tu app.
