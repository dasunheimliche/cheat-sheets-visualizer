---
title: "1.3 - Variables de Entorno y PATH"
description: "Guía paso a paso sobre qué son las variables de entorno, cómo usarlas en Python y qué es el PATH"
tags: ["python", "env vars", "os", "shell"]
---

## A - Concepto: ¿Qué es una Variable de Entorno? [🔴]

#### 1. **Introducción:**

Es una variable que vive **fuera** de tu código Python, directamente en el Sistema Operativo, pero que tu código puede leer.

#### 2. **Ejemplo:**

> Imagina que tu código es una receta de cocina. La **Variable de Entorno** no está escrita en la receta, es una nota pegada en la puerta del refrigerador que dice: `TEMPERATURA_HORNO="200"`.

#### 3. **Desarrollo:**

Son útiles para configuraciones (como contraseñas o claves de API) que no quieres escribir directamente dentro de tus archivos `.py`. Al estar "fuera", puedes cambiar la configuración sin tocar el código.

🔴 **Fundamental**: Sin entender esto, te costará configurar aplicaciones profesionales o desplegarlas en servidores.

---

## B - Creación en Terminal: Linux y macOS [🔵]

#### 1. **Introducción:**

Cómo crear una variable de entorno si usas Bash (común en Linux y Mac).

#### 2. **Ejemplo:**

```bash
# En tu terminal:
export MY_NAME="Wade Wilson"

# Para verificar que existe:
echo "Hello $MY_NAME"
# Resultado: Hello Wade Wilson
```

#### 3. **Desarrollo:**

Usamos el comando `export`. Esto hace que la variable esté disponible para otros programas que lances desde esa misma terminal.

🔵 **Específico**: Solo aplica si usas sistemas basados en Unix (Mac/Linux) o Git Bash en Windows.

---

## C - Creación en Terminal: Windows PowerShell [🔵]

#### 1. **Introducción:**

Cómo crear una variable de entorno si usas PowerShell en Windows.

#### 2. **Ejemplo:**

```powershell
# En tu terminal PowerShell:
$Env:MY_NAME = "Wade Wilson"

# Para verificar que existe:
echo "Hello $Env:MY_NAME"
# Resultado: Hello Wade Wilson
```

#### 3. **Desarrollo:**

¡Cuidado aquí! En Windows PowerShell la sintaxis cambia. Debes usar `$Env:` seguido del nombre. Es el equivalente exacto al `export` de Linux, pero escrito en "idioma Windows".

**Diferencia Clave:**

- Linux/Mac: `export NOMBRE="Valor"`
- Windows: `$Env:NOMBRE = "Valor"`

🔵 **Específico**: Solo para usuarios de Windows PowerShell.

---

## D - Lectura en Python: `os.getenv` [🔴]

#### 1. **Introducción:**

Cómo hacer que tu código Python lea esas variables que creaste afuera.

#### 2. **Ejemplo:**

```python
import os

# Leemos la variable que creamos en la terminal
name = os.getenv("MY_NAME")

print(f"Hello {name} from Python")
```

#### 3. **Desarrollo:**

Necesitas importar el módulo `os` (que viene con Python). La función `os.getenv("NOMBRE_VARIABLE")` va a buscar al sistema operativo y traerá el valor.

🔴 **Fundamental**: Es la forma estándar de leer configuraciones en Python.

---

## E - Robustez: Valores por Defecto [🟡]

#### 1. **Introducción:**

¿Qué pasa si la variable no existe? Evita errores definiendo un plan B (valor por defecto).

#### 2. **Ejemplo:**

```python
import os

# Si "MY_NAME" no existe en el sistema, usará "World"
name = os.getenv("MY_NAME", "World")

print(f"Hello {name} from Python")
```

**Explicación del ejemplo:**
El segundo argumento `"World"` es el salvavidas. Si olvidaste crear la variable `MY_NAME` en la terminal, Python usará `"World"` en lugar de devolver `None` (que es el comportamiento por defecto si no pones el segundo argumento).

#### 3. **Desarrollo:**

Siempre es buena práctica poner un valor por defecto para que tu programa no falle si se te olvida configurar el entorno.

🟡 **Importante**: Previene errores comunes de tipo "NoneType" y hace tu código más resistente.

---

## F - Variables "Inline" (Temporales) [🔵]

#### 1. **Introducción:**

Crear una variable que solo exista durante la fracción de segundo que se ejecuta tu programa.

#### 2. **Ejemplo:**

```bash
# En Linux/Mac:
MY_NAME="Wade Wilson" python main.py
```

**Explicación del ejemplo:**
Estás creando la variable y ejecutando el script en la misma línea.

1. El script `main.py` se ejecuta y lee "Wade Wilson".
2. Apenas termina el script, la variable `MY_NAME` **deja de existir**.

#### 3. **Desarrollo:**

Es muy útil cuando quieres probar algo rápido sin "ensuciar" tu terminal con variables que no volverás a usar.

🔵 **Específico**: Muy usado en servidores y pruebas rápidas, pero no es la forma habitual de desarrollo diario.

---

## G - Tipos de Datos: Todo es Texto [🟡]

#### 1. **Introducción:**

Las variables de entorno no saben de números ni booleanos, solo saben de texto.

#### 2. **Ejemplo:**

```python
import os

# Imagina que en la terminal hiciste: export MAX_USERS="10"
max_users = os.getenv("MAX_USERS")

print(type(max_users))
# Resultado: <class 'str'> (¡Es texto, no número!)

# Si necesitas hacer matemáticas, debes convertirlo:
number = int(max_users)
```

#### 3. **Desarrollo:**

Como las variables de entorno son del Sistema Operativo y no de Python, **siempre** se leen como cadenas de texto (`str`). Si necesitas un número, un booleano o una lista, **tienes que convertirlo manualmente** en tu código.

🟡 **Importante**: Fuente común de bugs. Recuerda siempre convertir los tipos si esperas números.

---

## H - Concepto: La variable `PATH` [🔴]

#### 1. **Introducción:**

Es una variable de entorno **especial** que contiene una lista de carpetas donde el sistema busca los programas ejecutables.

#### 2. **Ejemplo:**

Visualización del valor de `PATH`:

- **Linux/Mac:** `/usr/bin:/bin:/usr/sbin`
- **Windows:** `C:\Windows\System32;C:\Program Files\Python`

#### 3. **Desarrollo:**

Cuando escribes `python` en tu terminal, tu computadora no sabe mágicamente dónde está instalado Python. Lo que hace es mirar la variable `PATH`. Revisa la primera carpeta de la lista... ¿está ahí? No. Revisa la segunda... ¿está ahí? Sí. ¡Entonces lo ejecuta!

🔴 **Fundamental**: Entender el PATH es vital para saber por qué a veces la terminal dice "command not found".

---

## I - Estructura del PATH [🔵]

#### 1. **Introducción:**

Cómo se ve esta lista de carpetas por dentro (difiere según tu Sistema Operativo).

#### 2. **Ejemplo:**

- **Linux/Mac:** Usa **dos puntos** (`:`) para separar carpetas.
  - `/bin:/usr/local/bin`
- **Windows:** Usa **punto y coma** (`;`) para separar carpetas.
  - `C:\Windows;C:\Program Files`

#### 3. **Desarrollo:**

Es una sola cadena de texto larga. El sistema la corta en pedazos usando el separador (`:` o `;`) para obtener la lista de lugares donde buscar.

🔵 **Específico**: Detalle técnico importante para configurar tu entorno manualmente.

---

## J - Orden de Ejecución en el PATH [🟡]

#### 1. **Introducción:**

El orden importa: el sistema usa el primer programa que encuentra.

#### 2. **Ejemplo:**

Imagina que tu PATH es:
`C:\Python39;C:\Python312`

Si escribes `python`, el sistema ejecutará la versión **3.9**, porque está **primera** en la lista. Dejará de buscar aunque exista la 3.12 después.

#### 3. **Desarrollo:**

El sistema operativo recorre la lista de izquierda a derecha. Si encuentra una coincidencia, se detiene y la ejecuta.

🟡 **Importante**: Si tienes varias versiones de Python instaladas, la que aparece primero en el PATH es la que se usará.

---

## K - Instalación de Python y el PATH [🔵]

#### 1. **Introducción:**

Qué sucede cuando marcas la casilla "Add to PATH" al instalar Python.

#### 2. **Ejemplo:**

Al instalar, el instalador añade la ruta de tu nuevo Python (ej: `C:\opt\custompython\bin`) al **final** o al **inicio** de tu variable `PATH` existente.

Antes: `.../usr/bin`
Después: `.../usr/bin:/opt/custompython/bin`

#### 3. **Desarrollo:**

Esto permite que puedas escribir simplemente `python` en la terminal desde cualquier carpeta. Si no se agregara al PATH, tendrías que escribir la ruta completa cada vez (ej: `C:\opt\custompython\bin\python.exe`) para ejecutarlo.

🔵 **Específico**: Explica la "magia" de por qué los comandos funcionan globalmente.
