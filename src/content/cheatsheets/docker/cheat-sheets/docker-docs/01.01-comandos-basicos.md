---
title: "Comandos Básicos de Docker"
description: "Los comandos más usados en Docker"
---

## A - docker run 🔴 ⭐⭐⭐

#### 1. **Introducción:**

El comando más importante. Crea y ejecuta un contenedor.

```bash
docker run -d -p 8080:80 --name mi-app nginx
```

- `-d`: Ejecutar en segundo plano
- `-p`: Mapear puertos (host:container)
- `--name`: Nombre del contenedor

🔴 **Fundamental**: Sin `docker run`, no hay contenedores.

---

## B - docker ps y docker logs 🔴 ⭐⭐

#### 1. **Introducción:**

Ver contenedores activos y sus logs.

```bash
docker ps              # Contenedores activos
docker ps -a           # Todos los contenedores
docker logs mi-app     # Ver logs
docker logs -f mi-app  # Seguir logs en tiempo real
```

🔴 **Fundamental**: Esencial para debugging y monitoreo.
