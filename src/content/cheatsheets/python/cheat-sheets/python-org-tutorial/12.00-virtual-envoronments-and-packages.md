---
title: "12.1 - Entornos Virtuales y Paquetes en Python"
description: "Guía esencial para aislar proyectos y gestionar librerías con venv y pip"
tags: ["python", "venv", "pip", "virtual-environment"]
prompt-version: cheat-sheet-generator-1.1
---

## A - El Problema: Conflicto de Versiones [🔴]

#### 1. **Introducción:**

Imagina que tienes dos aplicaciones: la "App A" necesita la versión 1.0 de una librería, pero la "App B" necesita la versión 2.0 de esa misma librería.

#### 2. **Ejemplo:**

> **Situación:** Si instalas todo en el Python global de tu computadora, no puedes tener la versión 1.0 y la 2.0 instaladas al mismo tiempo.
> **Resultado:** Una de las dos aplicaciones dejará de funcionar.

#### 3. **Desarrollo:**

Python, por defecto, instala las librerías en un lugar común. Esto crea conflictos cuando diferentes proyectos tienen requisitos incompatibles (como necesitar una corrección de bugs antigua o una función nueva).

🔴 **Fundamental:** Entender esto es la base de por qué no debemos instalar todo "a lo loco" en nuestra máquina principal. Es el "por qué" antes del "cómo".

## B - La Solución: Entornos Virtuales [🔴]

#### 1. **Introducción:**

Un entorno virtual es simplemente una carpeta aislada (un directorio autocontenido) que tiene su propia instalación de Python y sus propias librerías.

#### 2. **Ejemplo:**

- **Carpeta Proyecto A:** Tiene su propio entorno virtual con la librería v1.0.
- **Carpeta Proyecto B:** Tiene su propio entorno virtual con la librería v2.0.
- **Tu sistema:** No se entera de nada y todo funciona en paz.

#### 3. **Desarrollo:**

Al usar entornos virtuales, si la Aplicación B necesita actualizarse a la versión 3.0, esto ocurre solo dentro de su "burbuja", sin romper la Aplicación A.

🔴 **Fundamental:** Es la herramienta estándar para mantener la cordura en el desarrollo con Python.

## C - Creación del Entorno con `venv` [🔴]

#### 1. **Introducción:**

Usamos el módulo `venv` (que ya viene con Python) para crear esta carpeta mágica.

#### 2. **Ejemplo:**

```bash
# Estructura: python -m venv <nombre-del-directorio>
python -m venv tutorial-env
```

**Explicación del ejemplo:**
Este comando crea una carpeta llamada `tutorial-env` en tu ubicación actual. Dentro, copia el intérprete de Python y crea la estructura necesaria. Nota: Instalará la misma versión de Python con la que ejecutaste el comando (si usaste `python3.12`, el entorno será 3.12).

🔴 **Fundamental:** Es el primer paso físico para empezar cualquier proyecto profesional.

## D - Convención de Nombres: `.venv` [🟡]

#### 1. **Introducción:**

Aunque puedes llamar a tu entorno como quieras, es muy común llamarlo `.venv`.

#### 2. **Ejemplo:**

```bash
python -m venv .venv
```

#### 3. **Desarrollo:**

Se usa `.venv` por dos razones prácticas:

1.  El punto `.` al inicio hace que la carpeta esté oculta en muchos sistemas (para que no moleste visualmente).
2.  Evita conflictos con archivos de variables de entorno llamados `.env` (que son otra cosa distinta).

🟡 **Importante:** Seguir convenciones facilita que otros programadores entiendan tu proyecto rápidamente.

## E - Activación del Entorno (Windows vs Unix) [🔴]

#### 1. **Introducción:**

Crear el entorno no basta; tienes que "entrar" en él. Aquí es donde muchos principiantes se traban porque el comando cambia según tu sistema operativo.

#### 2. **Ejemplo Comparativo:**

| Sistema Operativo | Comando a ejecutar en la terminal  |
| :---------------- | :--------------------------------- |
| **Windows**       | `tutorial-env\Scripts\activate`    |
| **Unix / MacOS**  | `source tutorial-env/bin/activate` |

**Explicación del ejemplo:**

- **Windows:** Usa backslashes `\` y la carpeta se llama `Scripts`.
- **Mac/Linux:** Usa slashes `/`, la carpeta se llama `bin`, y necesitas el comando `source` antes.
- _(Nota para usuarios de shells csh/fish: existen scripts alternativos como `activate.csh` o `activate.fish`)._

#### 3. **Desarrollo:**

Al activar, verás que el prompt de tu terminal cambia (generalmente aparece el nombre del entorno entre paréntesis). Esto confirma que ahora, cuando escribas `python`, usarás el Python de esa carpeta y no el de tu sistema.

🔴 **Fundamental:** Si no activas el entorno, estarás instalando cosas en tu sistema global por error. ¡Fíjate siempre en el paréntesis en tu terminal!

## F - Desactivación del Entorno [🔵]

#### 1. **Introducción:**

Cuando terminas de trabajar y quieres volver a tu terminal normal.

#### 2. **Ejemplo:**

```bash
deactivate
```

**Explicación del ejemplo:**
Simplemente escribe eso y presiona Enter. El paréntesis con el nombre del entorno desaparecerá.

🔵 **Específico:** Se usa cada vez que terminas una sesión de trabajo o cambias de proyecto.

## G - Gestión de Paquetes: `pip` [🔴]

#### 1. **Introducción:**

`pip` es el programa que usamos para instalar, actualizar y borrar librerías desde el "Python Package Index" (PyPI).

#### 2. **Ejemplo:**

```bash
# Estructura recomendada: python -m pip <comando>
python -m pip install requests
```

#### 3. **Desarrollo:**

Aunque a veces puedes escribir solo `pip`, el texto recomienda usar `python -m pip`. Esto asegura que estás usando el `pip` que pertenece exactamente al intérprete de Python actual (el de tu entorno virtual activo) y no otro `pip` perdido en tu sistema.

🔴 **Fundamental:** Sin `pip`, tendrías que descargar y configurar el código de las librerías manualmente.

## H - Instalación de Versiones Específicas [🟡]

#### 1. **Introducción:**

Puedes pedirle a `pip` la última versión de algo, o una versión exacta si tu proyecto es delicado.

#### 2. **Ejemplo:**

```bash
# Instalar la última versión disponible
python -m pip install novas

# Instalar una versión EXACTA (usando ==)
python -m pip install requests==2.6.0
```

**Explicación del ejemplo:**
El primer comando busca lo más nuevo. El segundo busca exactamente la versión 2.6.0. Si vuelves a ejecutar el comando de la versión específica, `pip` verá que ya la tienes y no hará nada.

🟡 **Importante:** En producción, solemos querer versiones específicas para evitar sorpresas si la librería cambia mañana.

## I - Actualizar y Desinstalar [🔵]

#### 1. **Introducción:**

Cómo mantener tus paquetes al día o deshacerte de lo que ya no usas.

#### 2. **Ejemplo:**

```bash
# Actualizar a la última versión
python -m pip install --upgrade requests

# Eliminar un paquete
python -m pip uninstall requests
```

**Explicación del ejemplo:**
`--upgrade` desinstalará la versión vieja e instalará la nueva automáticamente. `uninstall` te pedirá confirmación antes de borrarlo.

🔵 **Específico:** Tareas de mantenimiento rutinario.

## J - Inspección: `show` y `list` [🔵]

#### 1. **Introducción:**

A veces necesitas saber qué tienes instalado o detalles sobre una librería concreta.

#### 2. **Ejemplo:**

```bash
# Ver detalles de UN paquete
python -m pip show requests

# Ver TODOS los paquetes instalados
python -m pip list
```

**Explicación del ejemplo:**

- `show`: Te dice la versión, autor, licencia y dónde está guardado el paquete `requests`.
- `list`: Te da una lista simple de todo lo que hay en tu entorno (ej: `novas (3.1.1.3)`, `requests (2.7.0)`).

🔵 **Específico:** Útil para depurar o verificar qué versiones tienes realmente.

## K - Congelar Dependencias: `freeze` [🟡]

#### 1. **Introducción:**

`freeze` crea una lista exacta de tus paquetes instalados en un formato que `pip` puede leer. Es como tomar una "foto" de tu entorno.

#### 2. **Ejemplo:**

```bash
python -m pip freeze > requirements.txt
```

**Explicación del ejemplo:**
El comando `freeze` genera el texto (ej: `requests==2.7.0`). El símbolo `>` redirige ese texto a un archivo llamado `requirements.txt`.

#### 3. **Desarrollo:**

Este archivo `requirements.txt` es vital. Es lo que subes a tu control de versiones (GitHub, GitLab) para que tus compañeros sepan qué instalar.

🟡 **Importante:** Es la buena práctica estándar para compartir proyectos.

## L - Replicar el Entorno (`install -r`) [🟡]

#### 1. **Introducción:**

Cómo instalar todo lo necesario de golpe usando el archivo que creamos en el paso anterior.

#### 2. **Ejemplo:**

```bash
python -m pip install -r requirements.txt
```

**Explicación del ejemplo:**
`pip` leerá el archivo línea por línea e instalará cada paquete con su versión exacta.

#### 3. **Desarrollo:**

Esto permite que cualquier usuario (o servidor) tenga exactamente las mismas librerías que tú, garantizando que la aplicación funcione igual en todas partes.

🟡 **Importante:** Es el comando mágico que ejecuta un desarrollador nuevo cuando se descarga tu código por primera vez.
