---
title: "3 - Introducción Informal a Python"
description: "Guía esencial sobre números, textos, listas y primeros pasos en lógica de programación con Python."
tags: ["python", "basics", "data-types", "lists", "strings"]
prompt-version: cheat-sheet-generator-1.1
---

## A - El Entorno Interactivo [🔴 Fundamental]

#### 1. Introducción:

Python te habla a través de símbolos llamados "prompts" que te indican cuándo está listo para recibir órdenes o cuándo espera más información.

#### 2. Ejemplo:

```python
>>> 2 + 2      # El '>>>' significa "estoy listo, escribe algo"
4              # Esta línea sin prompt es la respuesta de Python
>>> if True:
...     print("Hola") # El '...' significa "continúa, no has terminado la orden"
```

#### 3. Desarrollo:

No debes escribir los símbolos `>>>` o `...` cuando copies código; son solo la forma en que Python te dice "es tu turno".

- **`>>>` (Prompt primario):** Python espera una nueva instrucción.
- **`...` (Prompt secundario):** Python sabe que estás escribiendo un bloque de código largo (como un bucle o una condición) y espera a que termines. Para terminar, usualmente debes dejar una línea en blanco.

## B - Comentarios [🔴 Fundamental]

#### 1. Introducción:

Son notas para ti o para otros programadores que Python ignora completamente al ejecutar el código.

#### 2. Ejemplo:

```python
# Esto es un comentario y Python no lo lee.
spam = 1  # Esto también es un comentario, va después del código.
text = "# Esto NO es un comentario porque está entre comillas."
```

#### 3. Desarrollo:

Se crean usando el símbolo `#` (hash). Todo lo que escribas desde el `#` hasta el final de esa línea es invisible para la ejecución del programa. Úsalos para aclarar _por qué_ hiciste algo, no _qué_ hiciste.

## C - Números y Operadores Básicos [🔴 Fundamental]

#### 1. Introducción:

Python funciona como una calculadora directa para sumas, restas y multiplicaciones, respetando el orden de operaciones (paréntesis primero).

#### 2. Ejemplo:

```python
>>> 2 + 2
4
>>> 50 - 5 * 6  # Primero multiplica, luego resta
20
>>> (50 - 5 * 6) / 4 # Los paréntesis fuerzan la resta primero
5.0
```

#### 3. Desarrollo:

Existen dos tipos principales de números que verás ahora:

- **`int` (Enteros):** Números sin coma (ej: `2`, `4`, `20`).
- **`float` (Flotantes):** Números con parte fraccionaria (ej: `5.0`, `1.6`).

## D - Los Tres Tipos de División [🔴 Fundamental]

#### 1. Introducción:

¡Cuidado aquí! En Python la división no es única; tienes tres herramientas diferentes según el resultado que necesites.

#### 2. Ejemplo:

```python
>>> 17 / 3    # 1. División Clásica: SIEMPRE devuelve un float (con decimales)
5.666666666666667

>>> 17 // 3   # 2. División de Piso (Floor): Corta los decimales, devuelve entero
5

>>> 17 % 3    # 3. Módulo (Residuo): Lo que sobra de la división
2
```

#### 3. Desarrollo:

- **`/` (División Clásica):** Úsala cuando quieras el resultado exacto. **Ojo:** Incluso si divides `4 / 2`, el resultado será `2.0` (un float), no `2`.
- **`//` (Floor Division):** Úsala cuando solo te interese la parte entera del resultado. No redondea al más cercano, simplemente "tira a la basura" los decimales.
- **`%` (Módulo):** Útil para saber si un número es par (residuo 0) o impar, o para ciclos.

## E - Potencias [🟡 Importante]

#### 1. Introducción:

Para elevar un número a una potencia, usa dos asteriscos `**`, no el símbolo `^` (que hace otra cosa en programación).

#### 2. Ejemplo:

```python
>>> 5 ** 2  # 5 al cuadrado
25
>>> 2 ** 7  # 2 elevado a la 7
128
```

## F - Variables y Asignación [🔴 Fundamental]

#### 1. Introducción:

El signo `=` no es una pregunta matemática ("¿es esto igual a esto?"), es una **orden**: "Guarda este valor en esta etiqueta".

#### 2. Ejemplo:

```python
>>> ancho = 20
>>> alto = 5 * 9
>>> ancho * alto
900
```

#### 3. Desarrollo:

Si intentas usar una variable que no has definido (asignado) previamente, Python te gritará un `NameError`.

- **Nota sobre `_` (Guion bajo):** Solo en el modo interactivo (la consola), la variable `_` guarda automáticamente el resultado de la última operación. Es útil para cálculos rápidos en cadena, pero **no** la uses en programas reales (scripts), trátala como "solo lectura".

## G - Definición de Strings (Texto) [🔴 Fundamental]

#### 1. Introducción:

Los textos (strings) pueden ir entre comillas simples `'...'` o dobles `"..."`; Python las trata exactamente igual.

#### 2. Ejemplo:

```python
>>> 'huevos y pan'
'huevos y pan'
>>> "huevos y pan"  # Es lo mismo
'huevos y pan'
>>> "Doesn't"       # Usa dobles si tu texto contiene una simple adentro
"Doesn't"
```

#### 3. Desarrollo:

Si necesitas poner una comilla dentro de un texto que usa el mismo tipo de comilla, debes "escaparla" usando una barra invertida `\` (ej: `'doesn\'t'`). Esto le dice a Python: "la siguiente comilla es texto, no el fin del string".

## H - `print()` vs. Salida Cruda [🟡 Importante]

#### 1. Introducción:

La consola te muestra los datos en su forma "cruda" (con comillas y códigos), `print()` te los muestra formateados para humanos.

#### 2. Ejemplo:

```python
>>> s = 'Primera linea.\nSegunda linea.'
>>> s  # Salida cruda (muestra el código \n)
'Primera linea.\nSegunda linea.'
>>> print(s) # Salida interpretada (hace el salto de línea)
Primera linea.
Segunda linea.
```

## I - Raw Strings y Multilínea [🔵 Específico]

#### 1. Introducción:

Hay trucos para manejar textos complejos, como rutas de archivos o párrafos largos.

#### 2. Ejemplo:

```python
# Raw Strings (r''): Ignoran los caracteres especiales como \n
print(r'C:\nombre\carpeta') # La 'n' no crea nueva línea gracias a la 'r'

# Multilínea ("""..."""): Permiten escribir en varios renglones
print("""
Uso: algo [OPCIONES]
     -h     Ayuda
""")
```

#### 3. Desarrollo:

- **Raw Strings (`r'...'`):** La `r` antes de la comilla es vital para rutas de Windows (`C:\...`) donde las barras invertidas abundan.
- **Multilínea:** Los saltos de línea que escribas se incluyen automáticamente en el texto.

## J - Concatenación de Strings [🟡 Importante]

#### 1. Introducción:

Puedes pegar textos usando `+`, repetirlos con `*`, o simplemente poniéndolos uno al lado del otro (solo si son literales).

#### 2. Ejemplo:

```python
>>> 3 * 'un' + 'ium'   # Repite 'un' 3 veces y pega 'ium'
'unununium'

>>> 'Py' 'thon'        # Yuxtaposición automática (Solo funciona con literales)
'Python'

>>> prefix = 'Py'
>>> prefix + 'thon'    # Con variables OBLIGATORIAMENTE usa '+'
'Python'
```

#### 3. Desarrollo:

**¡Trampa común!** La yuxtaposición (`'Py' 'thon'`) es útil para dividir textos largos en varias líneas de código, pero **no funciona con variables**. Si intentas `variable 'texto'`, te dará error. Usa siempre `+` si hay variables involucradas.

## K - Indexación de Strings (Acceder a caracteres) [🔴 Fundamental]

#### 1. Introducción:

Puedes obtener un carácter específico indicando su posición entre corchetes `[]`. Se empieza a contar desde 0.

#### 2. Ejemplo:

```python
word = 'Python'
>>> word[0]   # Primer carácter
'P'
>>> word[5]   # Sexto carácter
'n'
>>> word[-1]  # ¡Índices negativos! Cuenta desde el final
'n'
>>> word[-2]  # Penúltimo
'o'
```

#### 3. Desarrollo:

- **Índices Positivos:** `0` es el inicio.
- **Índices Negativos:** `-1` es el último carácter. Útil cuando no sabes qué tan largo es el texto.

## L - Slicing (Rebanado) [🔴 Fundamental]

#### 1. Introducción:

Permite extraer un sub-texto (una rebanada) usando `[inicio:fin]`.

#### 2. Ejemplo:

```python
word = 'Python'
>>> word[0:2]  # Desde 0 (incluido) hasta 2 (EXCLUIDO)
'Py'
>>> word[:2]   # Si omites el inicio, asume 0
'Py'
>>> word[4:]   # Si omites el final, va hasta el fondo
'on'
```

#### 3. Desarrollo:

**El Modelo Mental de los Bordes:**
Para no confundirte con qué se incluye y qué no, imagina que los índices apuntan a los **bordes** entre las letras, no a las letras en sí.

```text
 +---+---+---+---+---+---+
 | P | y | t | h | o | n |
 +---+---+---+---+---+---+
 0   1   2   3   4   5   6
-6  -5  -4  -3  -2  -1
```

Si pides `word[2:4]`, obtienes lo que está entre la pared 2 y la pared 4 (`th`).

- **Nota:** Los índices fuera de rango dan error al acceder (`word[42]`), pero en slicing se manejan suavemente (`word[4:42]` simplemente te da hasta donde llegue).

## M - Inmutabilidad de Strings [🔴 Fundamental]

#### 1. Introducción:

Una vez creado un string, **no puedes cambiarlo**. Son de "solo lectura".

#### 2. Ejemplo:

```python
word = 'Python'
# word[0] = 'J'  <-- ESTO DARÁ ERROR (TypeError)

# Forma correcta: Crear un string NUEVO
nuevo_word = 'J' + word[1:]
```

## N - Listas: Definición y Mutabilidad [🔴 Fundamental]

#### 1. Introducción:

Las listas agrupan valores (pueden ser de distintos tipos) entre corchetes `[]`. A diferencia de los strings, las listas **sí se pueden cambiar** (son mutables).

#### 2. Ejemplo:

```python
cubos = [1, 8, 27, 65, 125] # Hay un error, 4**3 es 64, no 65
cubos[3] = 64               # ¡Podemos corregirlo directamente!
print(cubos)
# [1, 8, 27, 64, 125]
```

#### 3. Desarrollo:

Las listas soportan indexación (`lista[0]`) y slicing (`lista[:2]`) y concatenación (`+`) igual que los strings.

## O - Método `append()` [🟡 Importante]

#### 1. Introducción:

Es la forma estándar de agregar elementos nuevos al **final** de una lista existente.

#### 2. Ejemplo:

```python
cubos.append(216)  # Agrega el cubo de 6
cubos.append(343)  # Agrega el cubo de 7
```

## P - Asignación vs. Copia de Listas [🔴 Fundamental]

#### 1. Introducción:

**¡Peligro de principiante!** Asignar una lista a otra variable (`b = a`) **NO** crea una copia independiente. Ambas variables apuntan a la **misma** lista en memoria.

#### 2. Ejemplo:

```python
rgb = ["Rojo", "Verde", "Azul"]
rgba = rgb             # rgba es solo un "apodo" para la misma lista rgb

rgba.append("Alfa")    # Modificamos rgba...
print(rgb)             # ¡rgb TAMBIÉN cambió!
# ["Rojo", "Verde", "Azul", "Alfa"]
```

#### 3. Desarrollo:

Si quieres una copia independiente para poder modificarla sin afectar la original, debes hacer una **Shallow Copy** (copia superficial) usando slicing completo:

```python
copia_correcta = rgb[:] # El [:] crea una lista nueva con los mismos datos
```

## Q - Modificar Listas con Slicing [🔵 Específico]

#### 1. Introducción:

Puedes cambiar, borrar o vaciar secciones enteras de una lista asignando valores a un slice.

#### 2. Ejemplo:

```python
letras = ['a', 'b', 'c', 'd']
letras[2:4] = ['C', 'D']     # Reemplazar
letras[2:4] = []             # Borrar esos elementos
letras[:] = []               # Vaciar la lista entera
```

## R - Listas Anidadas [🔵 Específico]

#### 1. Introducción:

Una lista puede contener otras listas adentro. Se accede usando múltiples corchetes.

#### 2. Ejemplo:

```python
a = ['a', 'b']
n = [1, 2]
x = [a, n]      # [['a', 'b'], [1, 2]]
print(x[0][1])  # Accede a la primera lista (0), segundo elemento (1) -> 'b'
```

## S - Primeros Pasos de Lógica: Fibonacci y `while` [🟡 Importante]

#### 1. Introducción:

Podemos combinar variables y bucles para crear lógica real.

#### 2. Ejemplo:

```python
# Serie de Fibonacci
a, b = 0, 1              # 1. Asignación Múltiple
while a < 10:            # 2. Bucle while
    print(a)
    a, b = b, a+b        # 3. Evaluación derecha-izquierda
```

#### 3. Desarrollo:

Aquí ocurren tres cosas clave:

1.  **Asignación Múltiple:** `a, b = 0, 1` asigna `0` a `a` y `1` a `b` simultáneamente.
2.  **Bucle `while`:** Ejecuta el código indentado mientras la condición (`a < 10`) sea verdadera.
    - **Truthiness:** En Python, cualquier número distinto de cero es `True`. Cualquier secuencia no vacía es `True`. El 0 y las secuencias vacías son `False`.
3.  **Evaluación:** En `a, b = b, a+b`, Python calcula **primero** todo lo de la derecha (usando los valores viejos) y **luego** asigna a la izquierda.

## T - Indentación [🔴 Fundamental]

#### 1. Introducción:

En Python, los espacios al inicio de la línea no son estética, son **reglas**. Definen qué código pertenece a qué bloque (como dentro de un `while`).

#### 2. Ejemplo:

```python
while a < 10:
    print(a)     # Está DENTRO del bucle porque está indentado
print("Fin")     # Está FUERA del bucle (se ejecuta al terminar)
```

#### 3. Desarrollo:

Debes usar la misma cantidad de espacios (o tabulaciones) para cada línea dentro del mismo bloque. Si mezclas o cambias la cantidad, Python dará error.

## U - `print()` Avanzado [🔵 Específico]

#### 1. Introducción:

La función `print()` puede recibir varios argumentos separados por comas y controlar cómo termina la línea.

#### 2. Ejemplo:

```python
i = 256 * 256
print('El valor es', i)  # Inserta un espacio automático entre argumentos
# Salida: El valor es 65536

a, b = 0, 1
print(a, end=',')        # end=',' evita el salto de línea y pone una coma
print(b)
# Salida: 0,1
```
