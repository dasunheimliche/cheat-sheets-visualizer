---
title: "2 - El Intérprete de Python"
description: "Guía esencial sobre cómo invocar, controlar y configurar el entorno del intérprete de Python."
tags: ["python", "interpreter", "cli", "encoding"]
prompt-version: cheat-sheet-generator-1.1
---

## A - Invocando al Intérprete [🔴]

#### 1. **Introducción:**

Es la acción de "llamar" a Python desde tu terminal para que empiece a trabajar.

#### 2. **Ejemplo:**

```bash
# En Unix/Linux (generalmente)
python3.14

# En Windows (si tienes el lanzador py.exe)
py
```

#### 3. **Desarrollo:**

Dependiendo de tu sistema operativo, el comando puede variar ligeramente. En Unix, suele vivir en `/usr/local/bin/python3.14`. En Windows, si usaste la Microsoft Store, tendrás `python3.14`, pero el lanzador `py` es muy común y práctico.
_Nota paranoica:_ Si escribes el comando y te dice "command not found", no entres en pánico; probablemente necesitas ajustar el "path" (la ruta de búsqueda) de tu sistema, pero eso es un tema de configuración del sistema operativo, no de Python en sí.

🔴 **Fundamental**: Si no puedes abrirlo, ¡no puedes programar!

## B - Salir del Intérprete (Escape de Emergencia) [🔴]

#### 1. **Introducción:**

Cómo cerrar Python y volver a tu terminal de comandos normal sin cerrar la ventana entera.

#### 2. **Ejemplo:**

```python
# Opción 1: El comando universal (funciona siempre)
quit()

# Opción 2: Atajos de teclado (más rápido)
# En Unix/Linux/Mac: Presiona [Control] + [D]
# En Windows: Presiona [Control] + [Z]
```

#### 3. **Desarrollo:**

Muchos principiantes entran a Python y luego no saben cómo salir, quedando "atrapados". El intérprete necesita una señal de "Fin de Archivo" (EOF). Si los atajos de teclado fallan, escribir `quit()` es tu salvavidas garantizado.

🔴 **Fundamental**: Saber entrar es importante, saber salir es vital para tu salud mental.

## C - Verificación de Edición de Línea (El Test del Beep) [🔵]

#### 1. **Introducción:**

Una prueba rápida para saber si puedes usar flechas, historial y autocompletado, o si estás en un modo "tonto" donde solo funciona el borrar.

#### 2. **Ejemplo:**

**Acción:** Abre Python y presiona `Control` + `P`.

**Resultados:**

- **Si suena un "Beep" (o no escribe nada):** ¡Genial! Tienes edición interactiva (gracias a GNU Readline).
- **Si aparece `^P` en pantalla:** Malas noticias. No tienes edición avanzada; solo podrás usar "backspace" para corregir la línea actual.

#### 3. **Desarrollo:**

Esto depende de cómo se instaló Python en tu sistema y si soporta la librería GNU Readline. Es bueno saberlo antes de intentar buscar comandos anteriores con la flecha hacia arriba y frustrarte porque aparecen caracteres extraños.

🔵 **Específico**: Útil para diagnóstico rápido cuando estás en una máquina ajena o un servidor.

## D - Modos de Operación: Interactivo vs. Script [🟡]

#### 1. **Introducción:**

Python actúa diferente dependiendo de si le hablas cara a cara (interactivo) o si le das una lista de tareas (script).

#### 2. **Ejemplo:**

```bash
# MODO INTERACTIVO (tty conectado)
# Python lee y responde línea por línea.
$ python3.14

# MODO SCRIPT (archivo como argumento)
# Python lee todo el archivo y lo ejecuta.
$ python3.14 mi_programa.py
```

#### 3. **Desarrollo:**

El intérprete detecta automáticamente el contexto. Si la entrada estándar está conectada a una terminal (tty), se pone "conversacional". Si le das un nombre de archivo, se pone "ejecutivo", hace el trabajo y se cierra.

🟡 **Importante**: Entender esta dualidad es base para desarrollar (script) y probar (interactivo).

## E - Ejecución de Comandos en Línea (Flag `-c`) [🔵]

#### 1. **Introducción:**

Ejecuta un pequeño fragmento de código Python directamente desde la terminal, sin crear un archivo.

#### 2. **Ejemplo:**

```bash
# Nota las comillas alrededor del comando
python3.14 -c "print('Hola desde la terminal')"
```

#### 3. **Desarrollo:**

La opción `-c` (command) funciona igual que en la shell de Unix.
_Advertencia paranoica:_ Dado que el código Python suele usar espacios y caracteres especiales, **siempre** encierra tu comando entre comillas (usualmente comillas dobles `"` en la terminal) para que la terminal no se confunda y crea que son instrucciones para ella.

🔵 **Específico**: Muy útil para pruebas rápidas de una sola línea o configuraciones.

## F - Ejecución de Módulos (Flag `-m`) [🟡]

#### 1. **Introducción:**

Ejecuta un módulo de librería (instalado en tu sistema) como si fuera un script principal.

#### 2. **Ejemplo:**

```bash
# Ejecuta el módulo 'timeit' para medir tiempos
python3.14 -m timeit -s "setup" "code"

# O el famoso servidor web simple
python3.14 -m http.server
```

#### 3. **Desarrollo:**

Busca el módulo en la ruta de librerías de Python y lo ejecuta. Es más seguro y preciso que tratar de adivinar dónde está guardado el archivo `.py` de esa librería en tu disco duro.

🟡 **Importante**: Es la forma estándar de ejecutar herramientas que vienen con Python (como `venv`, `pip`, `http.server`).

## G - Modo Interactivo Post-Script (Flag `-i`) [🔵]

#### 1. **Introducción:**

Le dice a Python: "Ejecuta este script, pero no te cierres al terminar; quédate abierto para que pueda investigar".

#### 2. **Ejemplo:**

```bash
# Ejecuta el script y luego te da el prompt >>>
python3.14 -i mi_script_con_errores.py
```

#### 3. **Desarrollo:**

Normalmente, cuando un script termina, Python se cierra. Con `-i`, entras en modo interactivo conservando todas las variables y funciones que el script definió.
_Tip Pedagógico:_ ¡Es una herramienta de depuración (debugging) increíble! Si tu script falla, usa `-i` para inspeccionar el "cadáver" de tus variables y ver qué pasó.

🔵 **Específico**: Una joya oculta para encontrar errores sin usar debuggers complejos.

## H - Paso de Argumentos (`sys.argv`) [🟡]

#### 1. **Introducción:**

Es la forma en que tu script puede leer las palabras que escribiste después del nombre del archivo en la terminal.

#### 2. **Ejemplo:**

```python
# Contenido de mi_script.py:
import sys
print(sys.argv)

# En la terminal ejecutas:
# $ python3.14 mi_script.py hola 123
```

**Resultado:** `['mi_script.py', 'hola', '123']`

#### 3. **Desarrollo:**

Python convierte todo lo que escribes en la línea de comandos en una **lista de cadenas de texto (strings)**. Esta lista vive en el módulo `sys` y se llama `argv`.
_Ojo:_ Siempre es una lista de _strings_, incluso si escribes números.

🟡 **Importante**: Es la forma más básica de hacer que tus programas reciban información del exterior al arrancar.

## I - El Comportamiento de `sys.argv[0]` [🔵]

#### 1. **Introducción:**

El primer elemento de la lista (`index 0`) cambia su identidad dependiendo de _cómo_ invocaste a Python.

#### 2. **Ejemplo:**

| Forma de invocar       | Valor de `sys.argv[0]`            |
| :--------------------- | :-------------------------------- |
| `python script.py`     | `'script.py'` (Nombre del script) |
| `python -c "..."`      | `'-c'`                            |
| `python -m modulo`     | Ruta completa del módulo          |
| `python -` (stdin)     | `'-'`                             |
| `python` (interactivo) | `''` (Cadena vacía)               |

#### 3. **Desarrollo:**

Es fácil confundirse aquí. Recuerda: `sys.argv[0]` **no** es el primer argumento que _tú_ pasas, es la referencia al script o comando que se está ejecutando. Tus argumentos reales empiezan en `sys.argv[1]`.
_Nota:_ Las opciones de Python (como `-i` o `-c`) _no_ aparecen en `sys.argv`; el intérprete se las "come" antes de crear la lista.

🔵 **Específico**: Crucial si estás escribiendo herramientas de línea de comandos (CLI).

## J - Prompts del Modo Interactivo [⚪]

#### 1. **Introducción:**

Las señales visuales que te da Python para indicarte que está esperando órdenes.

#### 2. **Ejemplo:**

```python
>>> x = True      # Prompt Primario: "Estoy listo para una nueva orden"
>>> if x:
...     print(x)  # Prompt Secundario: "Estoy esperando que termines el bloque"
```

#### 3. **Desarrollo:**

- `>>>` (Primario): Python está listo y esperando.
- `...` (Secundario): Python sabe que dejaste una instrucción a medias (como un `if` o una función) y espera que termines de escribir el bloque indentado. Necesitas dar un "Enter" extra en blanco para decirle "ya terminé".

⚪ **Raramente usado**: No lo "usas", simplemente aparece, pero es vital reconocer qué te está pidiendo el intérprete.

## K - Codificación de Fuente (Default UTF-8) [🟡]

#### 1. **Introducción:**

Por defecto, Python 3 asume que tus archivos de código están escritos en UTF-8.

#### 2. **Ejemplo:**

```python
# Esto funciona perfectamente sin configuración extra:
mensaje = "¡Hola mundo! Ñandú y Pingüino 🐧"
```

#### 3. **Desarrollo:**

Esto significa que puedes usar caracteres especiales, tildes, emojis y alfabetos no ingleses en tus cadenas de texto (strings) y comentarios. Aunque Python lo permite en nombres de variables, la recomendación (convención) es usar solo caracteres ASCII para identificadores (nombres de variables/funciones) para asegurar portabilidad.

🟡 **Importante**: En el mundo moderno, UTF-8 es el estándar.

## L - Declaración de Codificación Personalizada [🔵]

#### 1. **Introducción:**

Cómo decirle a Python que tu archivo NO está en UTF-8, sino en otro formato (como Windows-1252).

#### 2. **Ejemplo:**

```python
# -*- coding: cp1252 -*-
print("Este archivo usa codificación Windows-1252")
```

#### 3. **Desarrollo:**

Para que esto funcione, debe ser la **primera línea** de tu archivo. Es un comentario "mágico". Tu editor de texto también debe guardar el archivo en esa codificación, o verás caracteres basura.
_Consejo de amigo:_ A menos que tengas una razón muy específica (sistemas legacy), quédate con UTF-8.

🔵 **Específico**: Solo necesario si trabajas con archivos antiguos o sistemas con codificaciones regionales específicas.

## M - Codificación con Shebang [🔵]

#### 1. **Introducción:**

La única excepción a la regla de "la codificación va en la primera línea".

#### 2. **Ejemplo:**

```python
#!/usr/bin/env python3
# -*- coding: cp1252 -*-

print("Script ejecutable en Unix con codificación especial")
```

#### 3. **Desarrollo:**

En sistemas Unix, la línea "Shebang" (`#!`) se usa para decirle al sistema operativo con qué programa abrir el archivo. Si usas Shebang, esta **debe** ir primera. En este caso, Python permite que la declaración de codificación baje a la **segunda línea**.

🔵 **Específico**: Común en scripts destinados a ejecutarse en servidores Linux/Unix.
