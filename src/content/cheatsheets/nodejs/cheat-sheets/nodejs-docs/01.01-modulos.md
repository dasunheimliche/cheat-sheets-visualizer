---
title: "Módulos en Node.js"
description: "Sistema de módulos CommonJS y ES Modules"
---

## A - require() y module.exports 🔴 ⭐⭐

#### 1. **Introducción:**

CommonJS es el sistema de módulos original de Node.js.

```javascript
// math.js
module.exports = {
  sum: (a, b) => a + b
};

// app.js
const math = require('./math');
console.log(math.sum(2, 3)); // 5
```

🔴 **Fundamental**: Aunque ES Modules está ganando terreno, CommonJS sigue siendo muy usado.

---

## B - ES Modules (import/export) 🔴 ⭐

#### 1. **Introducción:**

El sistema de módulos moderno de JavaScript, soportado nativamente en Node.js.

```javascript
// math.js
export const sum = (a, b) => a + b;

// app.js
import { sum } from './math.js';
console.log(sum(2, 3)); // 5
```

🔴 **Fundamental**: El estándar moderno. Requiere `"type": "module"` en package.json.
