---
title: "Array Methods Completo"
description: "Todos los métodos de arrays en JavaScript"
order: 4
tags: ["javascript", "arrays", "methods"]
---

## A - push() y pop() 🔴 ⭐⭐⭐

#### 1. **Introducción:**

`push()` añade al final, `pop()` elimina del final. Modifican el array original.

```javascript
const arr = [1, 2, 3];
arr.push(4);    // [1, 2, 3, 4]
arr.pop();      // [1, 2, 3]
```

🔴 **Fundamental**: Operaciones básicas de pila (LIFO).

---

## B - shift() y unshift() 🔴 ⭐⭐

#### 1. **Introducción:**

`unshift()` añade al inicio, `shift()` elimina del inicio.

```javascript
const arr = [1, 2, 3];
arr.unshift(0); // [0, 1, 2, 3]
arr.shift();    // [1, 2, 3]
```

🔴 **Fundamental**: Operaciones de cola (FIFO).

---

## C - map() 🔴 ⭐⭐⭐

#### 1. **Introducción:**

Transforma cada elemento y devuelve un nuevo array.

```javascript
const nums = [1, 2, 3];
const doubled = nums.map(n => n * 2); // [2, 4, 6]
```

🔴 **Fundamental**: El método más usado para transformar datos.

---

## D - filter() 🔴 ⭐⭐⭐

#### 1. **Introducción:**

Filtra elementos que cumplan una condición.

```javascript
const nums = [1, 2, 3, 4, 5];
const evens = nums.filter(n => n % 2 === 0); // [2, 4]
```

🔴 **Fundamental**: Esencial para filtrar listas.

---

## E - reduce() 🔴 ⭐⭐

#### 1. **Introducción:**

Reduce un array a un único valor acumulado.

```javascript
const nums = [1, 2, 3, 4];
const sum = nums.reduce((acc, n) => acc + n, 0); // 10
```

🔴 **Fundamental**: El más poderoso pero también el más confuso.

---

## F - find() y findIndex() 🟡 ⭐⭐

#### 1. **Introducción:**

`find()` devuelve el primer elemento que cumple la condición. `findIndex()` devuelve su índice.

```javascript
const users = [{id: 1}, {id: 2}];
const user = users.find(u => u.id === 2);      // {id: 2}
const index = users.findIndex(u => u.id === 2); // 1
```

🟡 **Importante**: Muy usado para buscar en colecciones.

---

## G - some() y every() 🟡 ⭐

#### 1. **Introducción:**

`some()` verifica si al menos uno cumple. `every()` verifica si todos cumplen.

```javascript
const nums = [1, 2, 3, 4];
nums.some(n => n > 3);  // true
nums.every(n => n > 0); // true
```

🟡 **Importante**: Para validaciones rápidas.

---

## H - includes() 🟡 ⭐⭐⭐

#### 1. **Introducción:**

Verifica si un elemento existe en el array.

```javascript
const arr = [1, 2, 3];
arr.includes(2); // true
arr.includes(5); // false
```

🟡 **Importante**: Más legible que `indexOf !== -1`.

---

## I - slice() 🟡 ⭐⭐

#### 1. **Introducción:**

Extrae una porción del array sin modificar el original.

```javascript
const arr = [1, 2, 3, 4, 5];
arr.slice(1, 3); // [2, 3]
arr.slice(-2);   // [4, 5]
```

🟡 **Importante**: Copia superficial, útil para inmutabilidad.

---

## J - splice() 🟡 ⭐

#### 1. **Introducción:**

Añade, elimina o reemplaza elementos. Modifica el original.

```javascript
const arr = [1, 2, 3, 4];
arr.splice(1, 2, 'a', 'b'); // arr = [1, 'a', 'b', 4]
```

🟡 **Importante**: Versátil pero muta el array.

---

## K - concat() 🔵

#### 1. **Introducción:**

Une arrays sin modificar los originales.

```javascript
const a = [1, 2];
const b = [3, 4];
const c = a.concat(b); // [1, 2, 3, 4]
```

🔵 **Específico**: Alternativa: spread `[...a, ...b]`.

---

## L - join() 🔵 ⭐

#### 1. **Introducción:**

Convierte array a string con un separador.

```javascript
const arr = ['a', 'b', 'c'];
arr.join('-'); // "a-b-c"
```

🔵 **Específico**: Útil para generar strings.

---

## M - reverse() 🔵

#### 1. **Introducción:**

Invierte el orden. Modifica el original.

```javascript
const arr = [1, 2, 3];
arr.reverse(); // [3, 2, 1]
```

🔵 **Específico**: Cuidado, muta el array.

---

## N - sort() 🟡 ⭐

#### 1. **Introducción:**

Ordena el array. Por defecto como strings.

```javascript
const nums = [10, 2, 30];
nums.sort((a, b) => a - b); // [2, 10, 30]
```

🟡 **Importante**: Siempre usa función comparadora con números.

---

## O - flat() 🔵

#### 1. **Introducción:**

Aplana arrays anidados.

```javascript
const arr = [1, [2, [3, 4]]];
arr.flat(2); // [1, 2, 3, 4]
```

🔵 **Específico**: El número indica la profundidad.

---

## P - flatMap() 🔵

#### 1. **Introducción:**

Combina map + flat en una sola operación.

```javascript
const arr = [1, 2, 3];
arr.flatMap(n => [n, n * 2]); // [1, 2, 2, 4, 3, 6]
```

🔵 **Específico**: Más eficiente que map + flat separados.

---

## Q - fill() 🔵

#### 1. **Introducción:**

Rellena el array con un valor.

```javascript
const arr = new Array(3).fill(0); // [0, 0, 0]
```

🔵 **Específico**: Útil para inicializar arrays.

---

## R - from() 🔵

#### 1. **Introducción:**

Crea un array desde un iterable o array-like.

```javascript
Array.from('abc');           // ['a', 'b', 'c']
Array.from({length: 3}, (_, i) => i); // [0, 1, 2]
```

🔵 **Específico**: Muy útil para generar secuencias.

---

## S - at() 🔵

#### 1. **Introducción:**

Accede a elementos con índices negativos.

```javascript
const arr = [1, 2, 3, 4];
arr.at(-1); // 4 (último)
arr.at(-2); // 3 (penúltimo)
```

🔵 **Específico**: Más limpio que `arr[arr.length - 1]`.

---

## T - entries(), keys(), values() 🔵

#### 1. **Introducción:**

Devuelven iteradores para recorrer el array.

```javascript
const arr = ['a', 'b'];
[...arr.entries()]; // [[0, 'a'], [1, 'b']]
[...arr.keys()];    // [0, 1]
[...arr.values()];  // ['a', 'b']
```

🔵 **Específico**: Útil con for...of.
