---
title: "Promises en JavaScript"
description: "Guía completa sobre Promises y programación asíncrona"
order: 1
---

## A - ¿Qué es una Promise?: El Contrato del Futuro 🔴

#### 1. **Introducción:**

Una Promise es un objeto que representa el resultado eventual (éxito o fracaso) de una operación asíncrona. Piensa en ella como un "vale" que te dan cuando pides comida para llevar: te prometen que tendrás tu pedido, pero no ahora mismo.

#### 2. **Ejemplo:**

```javascript
const promesa = new Promise((resolve, reject) => {
  // Simulamos una operación que tarda 2 segundos
  setTimeout(() => {
    const exito = true;
    if (exito) {
      resolve("¡Datos obtenidos!");
    } else {
      reject("Error al obtener datos");
    }
  }, 2000);
});

promesa
  .then((resultado) => console.log(resultado))
  .catch((error) => console.error(error));
```

**→** Después de 2 segundos: `¡Datos obtenidos!`

#### 3. **Desarrollo:**

Una Promise puede estar en uno de tres estados:

| Estado | Descripción |
|--------|-------------|
| `pending` | Estado inicial, ni cumplida ni rechazada |
| `fulfilled` | La operación se completó exitosamente |
| `rejected` | La operación falló |

🔴 **Fundamental**: Las Promises son la base de la programación asíncrona moderna en JavaScript. Sin entenderlas, no podrás trabajar con APIs, bases de datos, o cualquier operación que tome tiempo.

---

## B - `.then()` y `.catch()`: Manejando el Resultado 🟡

#### 1. **Introducción:**

`.then()` se ejecuta cuando la Promise se resuelve exitosamente. `.catch()` se ejecuta cuando hay un error. Son como los dos caminos posibles después de hacer un pedido.

#### 2. **Ejemplo:**

```javascript
fetch("https://api.ejemplo.com/datos")
  .then((response) => response.json())
  .then((data) => console.log(data))
  .catch((error) => console.error("Error:", error));
```

**→** Los datos de la API o un mensaje de error

#### ⚠️ Cuidado con:

Olvidar el `.catch()`. Si una Promise falla y no hay `.catch()`, el error se "tragará" silenciosamente (o lanzará un warning en consola).

🟡 **Importante**: Siempre encadena un `.catch()` al final de tus cadenas de Promises para manejar errores de forma centralizada.

---

## C - `Promise.all()`: Esperando a Todos 🔵

#### 1. **Introducción:**

Cuando necesitas ejecutar varias Promises en paralelo y esperar a que TODAS terminen, usas `Promise.all()`.

#### 2. **Ejemplo:**

```javascript
const p1 = fetch("/api/usuarios");
const p2 = fetch("/api/productos");
const p3 = fetch("/api/config");

Promise.all([p1, p2, p3])
  .then(([usuarios, productos, config]) => {
    console.log("Todos los datos cargados");
  })
  .catch((error) => {
    console.error("Al menos una petición falló");
  });
```

**→** Se ejecuta `.then()` solo cuando las 3 terminan, o `.catch()` si alguna falla

🔵 **Específico**: Útil para cargar datos iniciales de una aplicación donde necesitas todo antes de renderizar.
